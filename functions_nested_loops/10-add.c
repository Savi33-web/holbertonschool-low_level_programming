#include "main.h"

/**
 * print_number - prints an integer using _putchar
 * @n: the integer to print
 */
void print_number(int n)
{return(a + b);
    }
{
    if (n < 0)
    {
        _putchar('-');
        n = -n;
    }
    if (n / 10)
        print_number(n / 10);
    _putchar((n % 10) + '0');
}

int mian (void);
{
    int result;

    result = add(89, 9);

    print_number(result);
    _putchar('\n');

    return (0);
}
