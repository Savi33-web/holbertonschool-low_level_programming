#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
  int a, b, product;
  int a => 0;
  int b => 0;
  
  product (num1 * num2)
    
 
  
    return (0);
}
